# Server-only ML gateway configuration (never expose as VITE_*)
ML_SERVICE_URL=http://127.0.0.1:8000
ML_REQUEST_TIMEOUT_MS=5000
ALLOW_FORECAST_FALLBACK=true

# Server-only Copilot gateway configuration (never expose as VITE_*)
COPILOT_SERVICE_URL=http://127.0.0.1:3001
COPILOT_SERVICE_TOKEN=
COPILOT_REQUEST_TIMEOUT_MS=10000
